{
  "name": "breaking-bad",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "webhooks": "lt --port 3000 -s breaking-bad",
    "types:generate": "turbo types:generate --ui stream"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.1.2",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "localtunnel": "^2.0.2",
    "supabase": "^1.192.5"
  }
}
